<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tseluikoartem.ening.yandexmobdevproject">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="net.hockeyapp.android.appIdentifier"
            android:value="${HOCKEYAPP_APP_ID}" />

        <activity android:name=".activities.GreetingActivity" />

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="31180e98341589f1bc92f1205f76d0e0bd26be31" />

        <activity android:name=".activities.AppInfoActivity" />
        <activity android:name=".activities.ThemeChoiceActivity" />
        <activity android:name=".activities.IconsAmountSetActivity" />
        <activity
            android:name=".activities.ScrollingLauncActivity"
            android:label=""
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.ScrollingListActivity"
            android:label="@string/title_activity_scrolling_list"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activities.SettingsActivity"
            android:label="@string/title_activity_preferencies" />

        <!-- <activity android:name=".activities.WelcomeActivity" -->
        <!-- android:theme="@style/SplashTheme"> -->


        <!-- </activity> -->

        <activity android:name=".activities.IconExtendedInformationActivity"
            android:theme="@style/Theme.AppCompat.Light.Dialog">

        </activity>
    </application>

</manifest>